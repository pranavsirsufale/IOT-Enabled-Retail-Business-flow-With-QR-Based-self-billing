# Copy to .env (or .env.local) and adjust for your backend
#
# When unset, the frontend uses relative /api paths (works with the Vite dev proxy
# and same-origin deployments).
VITE_API_URL=https://iot-v52t.onrender.com
# VITE_API_URL=http://localhost:8000
